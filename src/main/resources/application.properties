##Database configuration

##provide driver class name,here we are using postgresql database
spring.datasource.driverClassName=org.postgresql.Driver

##provie jdbc url for the database
##spring.datasource.url=jdbc:postgresql://localhost:5432/hrd?autoreconnect=true

##provide username and password
##spring.datasource.username=postgres
##spring.datasource.password=6698

##provie jdbc url for the database
spring.datasource.url=jdbc:postgresql://10.212.0.143:5432/hrd_emp_reimb_db?autoreconnect=true

##provide username and password
spring.datasource.username=hrd_emp_reimb_dev
spring.datasource.password=hrd_emp_reimb_dev

spring.messages.basename=messages/messages,messages/messages_errorcodes,config.i18n.messages
spring.messages.encoding=UTF-8


#management.endpoint.metrics.enabled=true

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
#management.endpoint.prometheus.enabled=true
#management.metrics.export.prometheus.enabled=true

logging.file.path=logs
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
 
spring.application.name=hrd-reimb-be
server.port=8093
eureka.client.service-url.defaultZone=http://localhost:8000/eureka/

